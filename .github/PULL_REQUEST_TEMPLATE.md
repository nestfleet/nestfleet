## Summary

<!-- What does this PR do? One paragraph max. Link the backlog item if applicable (e.g. Closes #123 or FEAT-XXX). -->

## Changes

<!-- Bullet list of the notable changes: new files, modified files, migrations, config vars. -->

-

## Test plan

- [ ] `npm run lint` passes (0 type errors)
- [ ] `npm test` passes (0 unit test failures)
- [ ] `npm run test:integration` passes locally (or N/A — explain why)
- [ ] New behaviour is covered by tests
- [ ] No new `any` types introduced

## Docs / config

- [ ] `.env.example` updated if new env vars added
- [ ] `CHANGELOG.md` updated if this is a user-facing change
- [ ] Migration is safe to run on a live DB (or N/A)

## Notes for reviewer

<!-- Anything the reviewer should pay particular attention to, or context that isn't obvious from the diff. -->
