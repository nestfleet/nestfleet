"use client";

/**
 * GracePeriodBanner — NF-PIVOT: no-op.
 *
 * Stripe grace period handling is part of the billing module (BILLING_ENABLED gate).
 * Re-implement once NF Stripe integration is live.
 */

export function GracePeriodBanner() {
  return null;
}
